@* Areas/Admin/Views/Genres/Details.cshtml *@

@model BookShop.Models.Genre

@{
    ViewData["Title"] = "Genre Details";
}

<div class="container">
    <h1 class="mb-4">Genre Details</h1>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Genre Information</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4">ID</dt>
                        <dd class="col-sm-8">@Model.GenreId</dd>

                        <dt class="col-sm-4">Genre Name</dt>
                        <dd class="col-sm-8">@Model.GenreName</dd>

                        <dt class="col-sm-4">Number of Books</dt>
                        <dd class="col-sm-8">@Model.Products.Count</dd>
                    </dl>
                </div>
                <div class="card-footer">
                    <div class="btn-group" role="group">
                        <a asp-action="Edit" asp-route-id="@Model.GenreId" class="btn btn-primary">Edit</a>
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (Model.Products.Any())
    {
        <div class="row mt-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Books in this Genre</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Title</th>
                                        <th>Author</th>
                                        <th>Price</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var book in Model.Products)
                                    {
                                        <tr>
                                            <td>@book.ProductId</td>
                                            <td>@book.Title</td>
                                            <td>@($"{book.Author.Name} {book.Author.LastName}")</td>
                                            <td>$@book.Price</td>
                                            <td>
                                                <a asp-area="Admin" asp-controller="Books" asp-action="Details" asp-route-id="@book.ProductId" class="btn btn-sm btn-info">Details</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>